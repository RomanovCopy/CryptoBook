<Window x:Class="CryptoBook.Views.SystemItemName_Editor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:interfaces="clr-namespace:CryptoBook.Interfaces"
        xmlns:viewmodels="clr-namespace:CryptoBook.ViewModels"
        xmlns:converters="clr-namespace:CryptoBook.Converters"
        xmlns:marcup="clr-namespace:CryptoBook.Markup"
        xmlns:local="clr-namespace:CryptoBook.Views"
        d:DataContext="{x:Type viewmodels:SystemItemName_Editor_ViewModel}"
        mc:Ignorable="d"
        Title="SystemItemName_Editor" MinHeight="200" MinWidth="400">
    <Window.DataContext>
        <marcup:ResolveViewModel ViewModelType="{x:Type interfaces:ISystemItemName_Editor_ViewModel}"/>
    </Window.DataContext>

    <Window.Width>
        <Binding Path="WindowWidth"  Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"/>
    </Window.Width>
    <Window.Height>
        <Binding Path="WindowHeight"  Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"/>
    </Window.Height>
    <Window.Top>
        <Binding Path="WindowTop"  Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"/>
    </Window.Top>
    <Window.Left >
        <Binding Path="WindowLeft"  Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"/>
    </Window.Left>
    <Window.WindowState>
        <Binding Path="WindowState" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"/>
    </Window.WindowState>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="25*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="15*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="Old Name:" Grid.Row="0" Grid.Column="1"/>
        <Label Content="Old Extension:" Grid.Row="0" Grid.Column="3"/>
        <TextBlock Text="{Binding OldName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1"/>
        <TextBlock Text="{Binding OldExtension, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="3"/>
        <Label Content="New Name:" Grid.Row="2" Grid.Column="1"/>
        <Label Content="New Extension:" Grid.Row="2" Grid.Column="3"/>
        <TextBox Text="{Binding NewName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Grid.Column="1"/>
        <TextBox Text="{Binding NewExtension, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Grid.Column="3"/>
    </Grid>
</Window>
