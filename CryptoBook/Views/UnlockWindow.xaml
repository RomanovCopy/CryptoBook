<Window x:Class="CryptoBook.Views.UnlockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Разблокировка CryptoBook"
        Width="420" Height="210"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource CurrentWindowBackground}"
        Foreground="{DynamicResource CurrentWindowForeground}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Ключ шифрования" FontWeight="SemiBold" FontSize="15"/>
        <PasswordBox x:Name="KeyBox" Grid.Row="2" MinHeight="30"/>
        <TextBlock x:Name="ErrorText" Grid.Row="3" Margin="0,8,0,0"
                   Foreground="{DynamicResource CurrentErrorForeground}"
                   TextWrapping="Wrap"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OpenButton" Content="Открыть" Width="100" Margin="0,0,8,0"
                    IsDefault="True" Click="OnOpen"/>
            <Button Content="Отмена" Width="100" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
