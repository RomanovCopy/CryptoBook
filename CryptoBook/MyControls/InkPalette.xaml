<UserControl x:Class="CryptoBook.MyControls.InkPalette"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:interfaces="clr-namespace:CryptoBook.Interfaces"
             xmlns:markup="clr-namespace:CryptoBook.Markup"
             xmlns:converters="clr-namespace:CryptoBook.Converters"
             mc:Ignorable="d"
             d:DataContext="{markup:ResolveViewModel ViewModelType={x:Type interfaces:IInkPaletteViewModel}}">
    <UserControl.DataContext>
        <markup:ResolveViewModel ViewModelType="{x:Type interfaces:IInkPaletteViewModel}"/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <Style x:Key="InkPaletteButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Margin" Value="1"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CurrentBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="250"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="{DynamicResource CurrentControlHoverBackground}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder"
                                        Property="Background"
                                        Value="{DynamicResource CurrentSelectionBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InkPaletteMenuItemStyle" TargetType="MenuItem">
            <Setter Property="Width" Value="26"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Margin" Value="1"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="Command" Value="{Binding ChooseCommand}"/>
            <Setter Property="ToolTip" Value="{Binding ToolTipText}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="MenuItemBorder"
                                Background="Transparent"
                                BorderBrush="{DynamicResource CurrentBorderColor}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <Border Background="{Binding Color, Converter={markup:ResolveConverter ConverterType={x:Type converters:ColorToColorConverter}}}"
                                    BorderBrush="{DynamicResource CurrentBorderColor}"
                                    BorderThickness="0.5"
                                    CornerRadius="2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="MenuItemBorder"
                                        Property="Background"
                                        Value="{DynamicResource CurrentControlHoverBackground}"/>
                                <Setter TargetName="MenuItemBorder"
                                        Property="BorderThickness"
                                        Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Height="28"
            Padding="2,1"
            Background="Transparent"
            BorderBrush="{DynamicResource CurrentBorderColor}"
            BorderThickness="1"
            CornerRadius="6"
            ToolTip="{Binding [Editor.InkPaletteHint], Source={StaticResource Res}}"
            ToolTipService.InitialShowDelay="500"
            AutomationProperties.Name="{Binding [Editor.InkPalette], Source={StaticResource Res}}">
        <ItemsControl ItemsSource="{Binding Colors}"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Style="{StaticResource InkPaletteButtonStyle}"
                            Command="{Binding ApplyCommand}"
                            ToolTip="{Binding ToolTipText}"
                            AutomationProperties.Name="{Binding ToolTipText}">
                        <Button.ContextMenu>
                            <ContextMenu ItemsSource="{Binding Choices}"
                                         Padding="4"
                                         Background="{DynamicResource CurrentWindowBackground}"
                                         BorderBrush="{DynamicResource CurrentBorderColor}"
                                         BorderThickness="1"
                                         ItemContainerStyle="{StaticResource InkPaletteMenuItemStyle}">
                                <ContextMenu.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Width="224"/>
                                    </ItemsPanelTemplate>
                                </ContextMenu.ItemsPanel>
                            </ContextMenu>
                        </Button.ContextMenu>
                        <Border Background="{Binding Color, Converter={markup:ResolveConverter ConverterType={x:Type converters:ColorToColorConverter}}}"
                                BorderBrush="{DynamicResource CurrentBorderColor}"
                                BorderThickness="0.5"
                                CornerRadius="2"/>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</UserControl>
